<%= form_for(@convo) do |f| %>
  <% if @convo.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@convo.errors.count, "error") %> prohibited this convo from being saved:</h2>
    <ul>
    <% @convo.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div>
	<%= f.label :privacy %><br />
	<%= t 'privacy.public' %>
	<%= f.radio_button :privacy_level, 1 , :checked => true %>
	<%= t 'privacy.confidential' %>
	<%= f.radio_button :privacy_level, 0 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
